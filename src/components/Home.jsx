import React from "react";
import { styles } from "../styles";
const Home = () => {
  return (
    <div className="h-screen flex items-center justify-center">
      <h1 className={`${styles.heroHeadText}`}>Edward Zhang</h1>
    </div>
  );
};

export default Home;
